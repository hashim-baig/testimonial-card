import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Card Testimonial Component – Next.js & Tailwind CSS",
  description: "A responsive testimonial card built with Next.js and Tailwind CSS. Features include pixel-perfect design, cross-browser support, fast performance, and accessibility. Ideal for showcasing frontend skills and modern web standards.",
    icons: {
      icon: '/favicon.png',
    }
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
